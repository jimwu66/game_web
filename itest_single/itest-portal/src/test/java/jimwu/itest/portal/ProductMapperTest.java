package jimwu.itest.portal;

import jimwu.itest.portal.mapper.ProductMapper;
import jimwu.itest.portal.model.Product;
import lombok.extern.slf4j.Slf4j;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import javax.annotation.Resource;

@SpringBootTest
@Slf4j
public class ProductMapperTest {

    /*
    @Resource
    private ProductMapper shopMapper;

    @Test
    void shopMapperTest(){
        Product shop = shopMapper.selectById(1);
        log.debug("prodcut 1 is {}",shop);
    }

     */
}
